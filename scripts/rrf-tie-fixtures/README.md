# `measure-rrf-ties` 的自驗 fixture

`#18 verify R1` 指出：初版的「探針驗過會動」只存在於一次性的對話操作裡，
**committed code 無法重跑**——而這個 repo 的判準是「不可能失敗的測試比沒有測試
更糟」，一個無法重跑的驗證在下一次改動時等於不存在。

這裡的每個檔案都是一個**假的 `ltm`**：它忽略參數、印出一段固定的 JSON。探針把它
當成真的 CLI 呼叫，於是整條「跑查詢 → 抽 token → 判平手 → 分桶統計」的路徑都被
執行，而輸入是手算得出答案的。

跑法見 `run-fixtures.sh`。
