# 基準查詢集（#63）。一行一條、`#` 開頭是註解、空行略過。
#
# 三件事，缺一不可（規則全文在 docs/measurements/README.md）：
#
# 1. **這是列舉，會漏。** 這幾條只主張「自行撰寫、未從語料抄、選定當下不在任何 tracked 檔案裡」；
#    說不出它們對真實使用的代表性（沒有評估集，#33）。
#    **前 5 名是否乾淨尚未在真實索引上驗過**（2026-09-07 實作當下 `~/bin/ltm` 剛換 inode、
#    鑰匙圈要重新授權，見 .claude/rules/anchor-key-in-probes.md）。第一次在 Claude Code 之外
#    跑 `measure-baseline.sh` 後，把日期與「N 行全 clean」寫回這一條；在那之前，本檔的乾淨
#    是宣稱不是量測。而且 `clean` 只代表前 k 名沒有 snippet 含這條查詢的原文（空白與大小寫摺疊後）
#    ——語料裡本來就逐字含它的實質 turn 會被判 `self`，而排在前面但不含原文的殘影會被判 `clean`；
#    判準會漏。
#
# 2. **本檔內容不得在 Claude Code session 內顯示。** 判準是「會不會落進被索引的欄位」：逐字稿
#    裡會進索引的是純字串的 `message.content`（使用者鍵入的 prompt 常是這一種，整段）、`text` block
#    （使用者輸入、Claude 的散文、compaction 摘要）與 tool_use 的七個 metadata 欄位
#    （`CorpusScanner.toolMetadataFields`：command / file_path / path / pattern / query / url /
#    description，各取前 200 字元）。所以：查詢出現在 Bash 命令列（`echo`、`ltm query <q>`、
#    `grep <q>`）、`Grep` 工具的 `pattern=`、`ltm_query` MCP 工具的 `query=`、任何工具的
#    `description=`、或被貼進／引述進對話——就進索引，下一次量測的前幾名就是「顯示它的那一刻」
#    （退役的前三條實測第一名全是那則量測命令）。`cat`／`Read` 本檔的**輸出**是 tool_result、今天
#    不被索引（#6 追蹤中、可能改變），但 Claude 看到之後引述它的那句散文一定被索引——所以一樣不做。
#    **編輯本檔在 Claude Code 之外的編輯器做。** session 內的 Write／Edit 今天索引安全（那些欄位不在七個
#    之列），但每一次都把整份檔案存進 jsonl（Write 的 content、Edit 的 originalFile、Read 的 attachment——
#    這是例子，判準與已存副本的計數在 README 規則 1）
#    ——#6 一收 tool payload，這組查詢集就整份作廢（README 規則 1 有已存副本的計數與查法）。
#    量測請跑 `scripts/measure-baseline.sh`，它只印編號。
#    `.gitattributes` 對本檔設了 `-diff`：它**只**改變 diff 生成——`git diff`／`git show <rev>`／
#    `git log -p` 只印「Binary files differ」、`git grep` 只印「Binary file … matches」；凡是把 blob 原樣
#    吐出來的命令（`git show <rev>:<path>`、`git blame`、`git cat-file -p`、`git archive`、`git diff --text`、
#    `git diff --no-index` 對 repo 外的路徑、`git format-patch` 的 base85 blob——`git apply` 一步還原）
#    照樣帶全文，這份例外清單會漏，判準是「它會不會把 blob 原樣吐出來」。要看內容在 session 之外做。
#
# 3. **量測輸出只印查詢集指紋、編號、耗時與 verdict，永不印查詢文字或命中內容。** 第一行是
#    `set sha256:<12 hex> k=<k>`（非註解行內容的指紋與 k，不含查詢文字；紀錄要連它一起引，退役換一條
#    之後同一個 `#N` 就是別的查詢），之後每列 `#N <ms>ms <verdict>`。
#    `#N` 就是本檔第 N 條非註解行（由 1 起算；去掉行首行尾的 ASCII 空白後，空行與 `#` 開頭不算；
#    全形空白與 NBSP 不算空白，測試斷言本檔沒有這類字元）。
#
# 選法（2026-09-07）：自行撰寫的通用技術詞彙（CLAUDE.md 隱私邊界：不從語料抄），中英混合、
# 含中文雙字與四字；退役的六條（見 README 的退役清單）不在此。**每次量測前都要重驗**
# （`measure-baseline.sh` 的 verdict 欄），選一次不代表永遠乾淨。
向量檢索 延遲
增量 掃描 游標
記憶策略 衰退
測試 fixture 密鑰
session resume 重複
embedding revision 不一致
鎖 競爭 建置
隱私 逐字稿 排除
