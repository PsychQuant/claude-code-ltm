# 基準查詢集（#63）。一行一條、`#` 開頭是註解、空行略過。
#
# 三件事，缺一不可（規則全文在 docs/measurements/README.md）：
#
# 1. **這是列舉，會漏。** 這幾條只保證「不被儀器自己污染」——選定當下前 5 名沒有量測命令
#    或查詢動作本身——說不出它們對真實使用的代表性（沒有評估集，#33）。
#
# 2. **本檔內容不得在 Claude Code session 內顯示。** 任何在 session 裡 `cat`／`echo`／
#    `Read` 這個檔、或把查詢原文貼進對話的動作，都會讓查詢字串進 `~/.claude/projects/**/*.jsonl`
#    → 進索引 → 下一次量測的第一名就是「顯示它的那一刻」。這正是它前身（三個活在命令列
#    歷史裡的查詢）失效的方式。編輯本檔請用 Write／Edit 工具（其 content 欄位不被索引）
#    或在 Claude Code 之外的編輯器；量測請跑 `scripts/measure-baseline.sh`，它只印編號。
#
# 3. **量測輸出只印編號、耗時與 clean/dirty，永不印查詢文字或命中內容。**
#    `#N` 就是本檔第 N 條非註解行（由 1 起算）。
#
# 選法（2026-09-07）：自行撰寫的通用技術詞彙（CLAUDE.md 隱私邊界：不從語料抄），中英混合、
# 含中文雙字與四字；退役的三條（見 README 的退役清單）不在此。**每次量測前都要重驗**
# 前 5 名乾淨（`measure-baseline.sh` 的 dirty 欄），選一次不代表永遠乾淨。
向量檢索 延遲
增量 掃描 游標
記憶策略 衰退
測試 fixture 密鑰
session resume 重複
embedding revision 不一致
鎖 競爭 建置
隱私 逐字稿 排除
