# Swift
.build/
.swiftpm/
*.xcodeproj
DerivedData/

# macOS
.DS_Store

# 索引與記憶資料一律不進 repo（它們住 ~/.claude-ltm/）
*.sqlite
*.sqlite-*
*.sqlite3
*.sqlite3-*
vectors.bin
state.json

# 第三方逐字內容一律不進 remote（見 CLAUDE.md 隱私邊界）
#
# `*.jsonl` 是**語料本身**的格式。它先前不在這份清單裡（#18 verify R1）——而這是
# 一份格式列舉，漏掉的偏偏是這個 repo 唯一真正處理的那種第三方逐字內容。#18 的
# 規模階梯需要把語料副本落到磁碟，於是這個缺口從理論變成可達：任何人照著量測
# 紀錄的重現步驟走、把子集放在工作樹下，就會把逐字語料 stage 進來而沒有任何阻擋。
# `scripts/sample-corpus-scales.py` 另有一道守衛（目的地不得在 repo 內），兩層都要。
*.jsonl
*.srt
*.mp3
*.m4a
*.wav
**/*_notes.md
fixtures/real/

# Spectra app data
.spectra/

# Claude Code worktrees（隔離用的暫時 checkout，不進版控）
.claude/worktrees/

# IDD 的本機工作檔：repo 指向設定與下載回來的 issue 附件。
# 前者是每台機器各自的（檔名就叫 local），後者是 GitHub 上已有的東西的副本。
.claude/.idd/
openspec/.vector-search.db*

# 編輯器本機設定（VSCode Swift extension 自動產生，機器相依）
.vscode/

# Python 匯入 script 當模組時產生的 bytecode 快取（純衍生物）
__pycache__/

# mcpb 的建置產物：universal binary 與打包好的 bundle 都是 release 產出，
# 不進 git（幾十 MB 的二進位）。`mcpb/manifest.json` 與 `server/.gitkeep` 追蹤。
mcpb/server/*
!mcpb/server/.gitkeep
mcpb/*.mcpb
mcpb/*.sha256

# 外部參考 repo（deep-research 指名的先例：git 的 racy-git 有界重算、
# watchman 的世代 token）。clone 進來是為了讀**實際出貨的實作**而不是二手摘要——
# 診斷引用它們的設計，就該對得起原始碼。不進版控：它們有自己的 history。
# 錨定在 repo 根：未錨定的 `repos/` 會匹配**任何深度**的同名目錄並安靜排除。
/repos/
