## 1. Presentation identifier plumbing

- [x] 1.1 Add a `presentation: PresentationID?` parameter (default `nil`) to `LTMService.record(kind:anchors:policy:store:now:)` and pass it through to every `Event` written in that call's loop, implementing every recorded presentation identifies the result list it originated from. Verify: `swift build` succeeds; existing callers that don't pass the new parameter still compile.
- [x] 1.2 In `LTMService.query(...)`, generate `PresentationID.random()` once (only when `recordEvents && eventStore != nil`) before calling `record(...)` for the `.shown` batch, and pass it as the new `presentation` argument — per design.md's decision that "the presentation identifier is generated once per `query()` call, in `record(...)`". Verify: new test in `Tests/LTMServiceTests/LTMServiceTests.swift` records two separate `query(...)` calls and asserts their `.shown` events carry two distinct, internally-consistent presentation identifiers — exercising two separate queries produce two separate presentation groups.
- [x] 1.3 Add `presentation: PresentationID?` to `QueryHit` and populate it from the same identifier generated in Task 1.2 — per design.md's decision that "`QueryHit` carries the presentation identifier as `presentation: PresentationID?`" — implementing a query result exposes the identifier it was recorded under. Verify: new test asserts each returned `QueryHit.presentation` matches the identifier attached to that hit's recorded `.shown` event in the store.

## 2. Spreading activation parameter and mechanism

- [x] 2.1 Add `spreadingActivationFactor: Double` to `ProjectionParameters` with a documented default and the same `precondition(isFinite && >= 0)` validation as the four existing weight parameters — per design.md's decision that "`spreadingActivationFactor` is a new, explicitly-unvalidated `ProjectionParameters` field". Verify: `swift build` succeeds; a new test in `Tests/LTMMemoryTests/ProjectionTests.swift` confirms a negative or non-finite factor traps at construction, mirroring the existing parameter validation tests.
- [x] 2.2 In `project(...)`, build a `presentationGroups: [PresentationID: Set<Anchor>]` map from every event carrying a non-nil `presentation` — per design.md's decision that "spreading activation is a second pass over `project(...)`'s existing per-event loop, not a rewrite of it" — as the first half of the human-like tier spreads reinforcement to co-presented anchors, one hop only requirement. Verify: covered by the integration test in Task 2.3 (this task has no independent externally-observable behavior on its own).
- [x] 2.3 Add the second pass over `project(...)`'s event loop: for every `opened`/`cited`/`pinned` event on a live anchor A with a presentation group G, add `spreadingActivationFactor * (A's decayed contribution for that event)` to `reinforcement[B]` for every other live anchor B in G. Verify: new test constructs a presentation group of three anchors, deliberately opens one, and asserts the other two show nonzero reinforcement scaled by `spreadingActivationFactor` relative to the opened anchor's own reinforcement — exercising a co-presented anchor with no direct interaction of its own gains reinforcement.
- [x] 2.4 Confirm spreading does not recurse past one hop: a spread contribution received by an anchor is not itself treated as a source event for a further spreading pass. Verify: new test constructs anchor A (opened) co-presented with B in group G1, and B co-presented with C in a *different* group G2 (A never co-presented with C), and asserts C receives zero reinforcement from A's event — exercising spreading does not recurse past one hop.
- [x] 2.5 Confirm `dismissed` events do not spread suppression to co-presented anchors. Verify: new test constructs a presentation group with one dismissed anchor and one anchor with no direct interaction, and asserts the second anchor's suppression is zero — exercising dismissal does not spread.

## 3. Documentation consistency

- [x] 3.1 Update `docs/memory-systems/README.md`'s "可插拔的是重排，不是記憶公式" section to remove or qualify the claim that a spreading-activation-capable strategy "目前寫不出來" (currently can't be written), since this change adds the mechanism — implementing spec consistency for the newly-added human-like tier spreads reinforcement to co-presented anchors requirement. Verify: manual content review confirming the README no longer contradicts the shipped mechanism; note that the underlying architectural limitation described in that section (projection formula is not on the pluggable axis) is unchanged and unaffected by this task — only the specific "can't write a co-occurrence-aware strategy" claim needs correcting.
