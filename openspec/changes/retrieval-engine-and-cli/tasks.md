## 1. Targets and corpus ingestion

- [x] 1.1 Wire the new targets in Package.swift: LTMIndex and LTMService libraries plus the single `ltm` binary with subcommands (executable target). Behavior: `swift build` produces an `ltm` executable; Package.swift declares no third-party package dependencies (system SQLite3, NaturalLanguage, Accelerate only). Verify: `swift build` succeeds; dependency review on Package.swift.
- [x] 1.2 Corpus reader in Sources/LTMIndex/ implementing "The corpus is read and never written" and "Incremental builds resume from a verified prefix": scan `~/.claude/projects/**/*.jsonl` read-only via `CorpusLocation`, tracking per-file prefix hash and processed byte count. Behavior: appended-only files are read from the recorded offset; prefix-mismatch files are re-parsed from zero; no write path targets the corpus root. Verify: LTMIndexTests incremental-resume tests (append case and rewrite case) and write-refusal test.
- [x] 1.3 Turn-level chunker implementing "Chunk granularity is one conversation turn with full pointer metadata": each chunk carries `(project, sessionId, uuid, timestamp)` and its text. Behavior: a 3-project / 50-turn fixture yields exactly 50 chunks, all four pointer fields non-empty. Verify: LTMIndexTests pointer-completeness test.

## 2. Index channels and publication

- [x] 2.1 SQLite index file realizing the design's derived index under ~/.claude-ltm/derived: FTS5 trigram and segment tables (tokenizer configuration from docs/measurements/2026-08-10-fts5-tokenizer.md), chunk metadata columns, and the stamps required by "The index declares its layout version and embedding revision", plus a startup capability probe that fails loudly naming the SQLite version when FTS5/trigram is unavailable. Behavior: schema is created on first build; layout-version mismatch triggers a from-scratch build; probe failure exits non-zero, never degrades silently. Verify: LTMIndexTests schema-creation, layout-version-mismatch, and probe-failure tests.
- [x] 2.2 Embedding channel: one `NLContextualEmbedding` vector per chunk written to a flat mmap-ed sidecar, embedding revision recorded in the index stamps. Behavior: vector count equals chunk count; the sidecar loads lazily. Verify: LTMIndexTests vector-count and revision-stamp tests.
- [x] 2.3 Atomic publication and single-writer lock: sidecar written to a temp path then renamed; a lock file under `~/.claude-ltm/derived` refuses concurrent builds. Behavior: a second concurrent build exits non-zero naming the lock; readers never observe a half-written sidecar. Verify: LTMIndexTests concurrent-build-refusal test.

## 3. Retrieval and facade

- [x] 3.1 Query execution implementing "A query fuses lexical and semantic channels by reciprocal rank" — the design's RRF fusion of FTS5 and embedding channels: trigram, segment, and brute-force cosine (Accelerate) rankings merged by reciprocal rank into banded `Candidate` values, satisfying "Every result carries the four-field pointer" on every fused hit. Behavior: fused list is exactly the union of channel rankings ordered by summed reciprocal ranks; unattributable results are dropped, never emitted partially. Verify: LTMServiceTests fusion test reproducing the spec's A/B/C example table plus pointer-presence assertions.
- [x] 3.2 LTMService facade with thin adapters implementing the staleness policy: incremental continue, revision refuse ("An embedding revision mismatch refuses the query", error naming `ltm build`), and "Strategy application goes through the MemoryStrategy seam with archival as default" — the design's default strategy is archival — with an injectable event sink backed by FileEventStore per event sink injectable, CLI records only with --record. Behavior: default query order equals fused order with zero displacement; cross-revision query fails without results. Verify: LTMServiceTests revision-refuse and zero-displacement tests.
- [x] 3.3 Invariant-2 acceptance tests implementing "The derived index is a pure derivative of the corpus": rebuild equivalence (`rm -rf ~/.claude-ltm/derived` + rebuild ⇒ identical query results on fixtures) and incremental ≡ full equivalence after appends. Behavior: both equivalences hold on the fixture corpus. Verify: named LTMServiceTests acceptance tests green.

## 4. CLI surface

- [x] 4.1 `ltm build [--full]` implementing "ltm build constructs the derivative and writes nowhere else": incremental by default, `--full` from nothing, writes contained under `~/.claude-ltm/`, non-zero exits naming the reason, concurrent builds refused via the lock. Behavior: fixture corpus tree is byte-identical after a build. Verify: corpus-untouched test plus a CLI invocation on the fixture corpus.
- [x] 4.2 `ltm query` implementing "ltm query prints pointered hits in human and JSON forms", "Scope resolution mirrors the retrieval default" ("Query scope defaults to one project and widens only explicitly"), and "Failure messages name their remediation": human output (rank, project, timestamp, snippet, pointer), `--json` objects carrying project/sessionId/uuid/timestamp/snippet/score/band, cwd→project scope default, refusal naming `--project`/`--all-projects` when unmappable, index-state failures naming `ltm build`. Behavior: JSON is machine-complete; ambiguous scope never widens silently. Verify: JSON-completeness and unmappable-scope CLI tests.
- [x] 4.3 Event recording implementing "Event recording is opt-in and off by default": `--record` appends one `shown` event per emitted hit through the facade's event sink; without it the event store is untouched. Behavior: 5 hits with `--record` append exactly 5 anchored events; default appends none. Verify: event-log-untouched and five-shown-events tests.

## 5. Documentation

- [x] 5.1 Update README status section and CLAUDE.md technical notes: the runnable path (`ltm build` / `ltm query`) exists, staleness and scope behavior described exactly as the Implementation Contract states, no performance claims beyond the already-cited docs/measurements/ records. Behavior: docs match shipped CLI behavior. Verify: content review against design.md Implementation Contract.
